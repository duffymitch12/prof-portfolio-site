

export default function ProgressBar({
    progress,
    title,
    description,
}: {
    progress: number,
    title: string,
    description?: string,
}) {

    return (
        <>

        </>
    )
};